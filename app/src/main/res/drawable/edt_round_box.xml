<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
      android:padding="40dp"
      android:shape="rectangle" >

      <!-- you can use any color you want I used here gray color -->
      <solid android:color="@android:color/transparent" />

      <corners
        android:bottomLeftRadius="@dimen/edt_round_btn"
        android:bottomRightRadius="@dimen/edt_round_btn"
        android:topLeftRadius="@dimen/edt_round_btn"
        android:topRightRadius="@dimen/edt_round_btn" />

      <!--//gradient is used to hide inner color-->
      <!--<gradient-->
      <!--android:angle="270"-->
      <!--android:type="linear" />-->
    <!--//outer boundery-->
      <stroke
        android:width="@dimen/dp1"
        android:color="@color/grey_blue_20opacity" />
    </shape>
